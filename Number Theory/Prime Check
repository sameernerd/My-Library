public static boolean isPrime(int n){
		if(n==1)return false;
		boolean flag=true;
		for(int i=2;i<=sqrt(n);i++){
			if(n%i==0){
				flag=false;
				break;
			}
		}
		if(flag)return true;
		else return false;
	}
